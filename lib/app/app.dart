import 'package:cash_machine/app/home_screen.dart';
import 'package:flutter/material.dart';

class CashApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomeScreen(),
    );
  }
}
